<div>
    <div class="container" data-ng-controller="PracticeController">
        <h2>{{model.province}} Practice Schedule</h2>
        
        <p class="panel-info">
            This page is not yet completed!!! It doesn't work
        </p>

        <table class="table table-striped table-condensed table-hover">
            <thead>
                <tr>
                    <th>Team</th>
                    <th>Alley</th>
                    <th>Time</th>
                </tr>
            </thead>
            <tbody>
                <tr data-ng-repeat="team in model.teams">
                    <td>{{team.Name}}</td>
                    <td><select data-ng-model="team.PracticeLocation" data-ng-options="location for location in model.practiceLocations"></select></td>
                    <td><select data-ng-model="team.PracticeTime" data-ng-options="time for time in model.allowedTimes"></select></td>
                </tr>
            </tbody>
        </table>

        <a href="#" data-ng-click="saveTravelPlans()" class="btn btn-primary btn-large pull-right">Save</a>
    </div>
</div>